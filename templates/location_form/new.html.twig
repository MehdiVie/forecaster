{% extends 'layout_boxed.html.twig' %}


{% block title %}New Form Location{% endblock %}

{% block content %}
    {{ form(form) }}
    {# {{form_start(form)}}

        <div class="row mb-3">
            {{form_label(form.name)}}
            <div class="col-sm-6">
                {{form_widget(form.name)}}
            </div>
        </div>

    {{form_end(form)}} #}
{% endblock %}
